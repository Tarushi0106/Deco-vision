# Copy to .env (gitignored).
# This service's own backend (detection/backend), NOT Parachute's.
VITE_DETECTION_API_BASE=http://127.0.0.1:8812
# Parachute's existing backend — used ONLY for camera list + the live video
# feed (/api/cameras, /ws/live/{id}), the same public endpoints a browser
# already uses for the main Parachute app.
VITE_PARACHUTE_API_BASE=http://127.0.0.1:8811
